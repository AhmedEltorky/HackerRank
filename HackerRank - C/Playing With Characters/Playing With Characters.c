#include stdio.h
#include string.h
#include math.h
#include stdlib.h

int main() {
	char ch;
	scanf(%c, &ch);
	printf(%c, ch);
	printf(n);

	char arr[20];
	scanf(%s, &arr);
	printf(%s, arr);
	printf(n);

	char arr2[100];
	scanf(n);
	scanf(%[^n]%c, &arr2);
	printf(%s, arr2);

	return 0;
}
